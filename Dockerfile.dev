# Development Dockerfile — hot reload with ts-node watch
FROM node:20-alpine
WORKDIR /app

ENV NODE_ENV=development

COPY package*.json ./
RUN npm ci

# Source code is mounted as a volume — no COPY needed here
# (see docker-compose.dev.yml volumes section)

EXPOSE 4000

CMD ["npm", "run", "start:dev"]
